spring:
  application:
    name: users-service
  profiles:
    active: test
  cloud:
    consul:
      enabled: false
application:
  network:
    gateway-url: http://172.25.0.10:8000
    services:
      products:
        name: products-service
        endpoints:
          list: products
      carts:
        name: carts-service
        endpoints:
          list: carts