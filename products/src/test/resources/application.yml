spring:
  application:
    name: products-service
  profiles:
    active: test
  banner:
    location: classpath:/banner.txt
  cloud:
    consul:
      enabled: false

application:
  network:
    gateway-url: http://172.25.0.10:8000
    services:
      carts:
        name: carts-service
        endpoints:
          add: carts
